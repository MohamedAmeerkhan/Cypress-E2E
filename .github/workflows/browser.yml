name: Chrome Browser E2E Testing
on: push
jobs:
  chrome:
    runs-on: windows-latest
    name: E2E on Chrome
    steps:
      - uses: actions/checkout@v4
      - uses: cypress-io/github-action@v6
        with:
          browser: chrome